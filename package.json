{
	"scripts": {
		"install": "lerna bootstrap",
		"clean": "lerna clean && rm -rf node_modules",
		"publish": "lerna publish --npm-client npm",
		"add": "lerna add @why-monorepo-demo/${added-package} --scope=@why-monorepo-demo/{$target-package}"
	},
	"private": true,
	"workspaces": [
		"**/packages/*"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/getElementsByName/why-monorepo-demo.git"
	},
	"devDependencies": {
		"lerna": "^3.16.4"
	},
	"name": "why-monorepo-demo"
}
