{
	"scripts":{
		"install": "lerna bootstrap",
		"clean": "lerna clean",
		"add": "lerna add @why-monorepo-demo/${package} --scope=@why-monorepo-demo/{$package}"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/getElementsByName/why-monorepo-demo.git"
	},
	"devDependencies": {
		"lerna": "^2.11.0"
	}
}